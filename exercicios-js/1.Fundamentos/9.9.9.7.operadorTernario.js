// Operadores Ternários

const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado';

console.log(resultado(10)); // Resultado Aprovado
console.log(resultado(5)); // Resultado Reprovado

// Primeira Parte => Expressão
// ? => Se for verdeiro retorna
// : => Se for falso retorna

